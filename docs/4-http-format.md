# Understanding the HTTP Format

1 - Open developer tools and jump to "Network" tab

2 - Make an HTTP request via GraphQL tool on `localhost:9000`

3 - Headers are:

```
method: POST,
content-type: application/json,
requestPayload: ~ a JSON object ~
```